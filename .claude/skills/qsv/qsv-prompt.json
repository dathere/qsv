{
  "name": "qsv-prompt",
  "version": "13.0.0",
  "description": "Open a file dialog to pick a file as input or save to an output file.",
  "category": "utility",
  "command": {
    "binary": "qsv",
    "subcommand": "prompt",
    "args": [],
    "options": [
      {
        "flag": "--base-delay-ms",
        "type": "string",
        "description": "The base delay in milliseconds to use when opening INPUT dialog. This is to ensure that the INPUT dialog is shown before/over the OUTPUT dialog when using the prompt command is used in both INPUT and OUTPUT modes in a single pipeline. [default: 200]",
        "default": "200"
      },
      {
        "flag": "--fd-output",
        "type": "flag",
        "description": "Write output to a file by using a save file dialog. Used when piping into qsv prompt. Mutually exclusive with --output."
      },
      {
        "flag": "--filters",
        "type": "string",
        "description": "The filter to use for the INPUT file dialog. Set to \"None\" to disable filters. Filters are comma-delimited file extensions. Defaults to csv,tsv,tab,ssv,xls,xlsx,xlsm,xlsb,ods. If the polars feature is enabled, it adds avro,arrow,ipc,parquet, json,jsonl,ndjson & gz,zst,zlib compressed files to the filter."
      },
      {
        "flag": "--help",
        "type": "flag",
        "description": "Display this message"
      },
      {
        "flag": "--msg",
        "type": "string",
        "description": "The prompt message to display in the file dialog title. When not using --fd-output, the default is \"Select a File\". When using --fd-output, the default is \"Save File As\"."
      },
      {
        "flag": "--output",
        "type": "string",
        "description": "Write output to <file> without showing a save dialog. Mutually exclusive with --fd-output."
      },
      {
        "flag": "--quiet",
        "type": "flag",
        "description": "Do not print --fd-output message to stderr."
      },
      {
        "flag": "--save-fname",
        "type": "string",
        "description": "The filename to save the output as when using --fd-output. [default: output.csv]",
        "default": "output.csv"
      },
      {
        "flag": "--workdir",
        "type": "string",
        "description": "The directory to start the file dialog in. [default: .]",
        "default": "."
      }
    ]
  },
  "hints": {
    "streamable": true,
    "memory": "constant"
  },
  "test_file": "https://github.com/dathere/qsv/blob/master/tests/test_prompt.rs"
}