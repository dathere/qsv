{
  "name": "qsv-sort",
  "version": "14.0.0",
  "description": "Sorts CSV data in lexicographical, natural, numerical, reverse, unique or random (with optional seed) order (Also see `extsort` & `sortcheck` commands).",
  "category": "utility",
  "command": {
    "binary": "qsv",
    "subcommand": "sort",
    "args": [
      {
        "name": "input",
        "type": "file",
        "required": true,
        "description": ""
      }
    ],
    "options": [
      {
        "flag": "--delimiter",
        "type": "string",
        "description": "The field delimiter for reading CSV data. Must be a single character. (default: ,)"
      },
      {
        "flag": "--faster",
        "type": "flag",
        "description": "When set, the sort will be faster. This is done by using a faster sorting algorithm that is not \"stable\" (i.e. the order of identical values is not guaranteed to be preserved). It has the added side benefit that the sort will also be in-place (i.e. does not allocate), which is useful for sorting large files that will otherwise NOT fit in memory using the default allocating stable sort."
      },
      {
        "flag": "--ignore-case",
        "type": "flag",
        "description": "Compare strings disregarding case"
      },
      {
        "flag": "--memcheck",
        "type": "flag",
        "description": "Check if there is enough memory to load the entire CSV into memory using CONSERVATIVE heuristics. Ignored if --random or --faster is set."
      },
      {
        "flag": "--natural",
        "type": "flag",
        "description": "Compare strings using natural sort order (treats numbers within strings as actual numbers, e.g. \"data1.txt\", \"data2.txt\", \"data10.txt\", as opposed to \"data1.txt\", \"data10.txt\", \"data2.txt\" when sorting lexicographically) https://en.wikipedia.org/wiki/Natural_sort_order"
      },
      {
        "flag": "--no-headers",
        "type": "flag",
        "description": "When set, the first row will not be interpreted as headers. Namely, it will be sorted with the rest of the rows. Otherwise, the first row will always appear as the header row in the output."
      },
      {
        "flag": "--numeric",
        "type": "flag",
        "description": "Compare according to string numerical value"
      },
      {
        "flag": "--output",
        "type": "string",
        "description": "Write output to <file> instead of stdout."
      },
      {
        "flag": "--random",
        "type": "flag",
        "description": "Randomize (scramble) the data by row"
      },
      {
        "flag": "--reverse",
        "type": "flag",
        "description": "Reverse order"
      },
      {
        "flag": "--rng",
        "type": "string",
        "description": "The RNG algorithm to use if --random is set. Three RNGs are supported:",
        "default": "standard"
      },
      {
        "flag": "--seed",
        "type": "string",
        "description": "Random Number Generator (RNG) seed to use if --random is set"
      },
      {
        "flag": "--select",
        "type": "string",
        "description": "Select a subset of columns to sort. See 'qsv select --help' for the format details."
      },
      {
        "flag": "--unique",
        "type": "flag",
        "description": "When set, identical consecutive lines will be dropped to keep only one line per sorted value."
      }
    ]
  },
  "hints": {
    "streamable": false,
    "memory": "full"
  }
}